# Example model configuration snippet for Hermes Agent
# Place under the `model:` section in /opt/data/config.yaml

model:
  default: "qwen/qwen3.7-max"  # Primary choice for agentic work
  # fallback_providers: []  # Optional: configure provider fallbacks
  # Alternative defaults based on task type:
  #   default: "deepseek/deepseek-v4-pro"  # Coding-heavy
  #   default: "moonshotai/kimi-k2.6"     # Large context analysis
  #   default: "nvidia/nemotron-3-super-120b-a12b"  # Stable backup (user requested)